openapi: 3.0.3
info:
  title: svcdesk API
  description: |
    ai-generated: 100% - generated by Gemini API
    Service Desk API for Lab 1. Handles ticket creation, priority matrix, and SLA tracking.
  version: 1.0.0
paths:
  /health:
    get:
      summary: Health check endpoint
      responses:
        '200':
          description: Service is healthy and running
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
  /tickets:
    post:
      summary: Create a new service desk ticket
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                priority:
                  type: string
                  enum: [P1, P2, P3, P4]
                vip:
                  type: boolean
      responses:
        '201':
          description: Ticket successfully created
  /tickets/{id}:
    patch:
      summary: Update ticket status (e.g., close or reopen)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                status:
                  type: string
                  enum: [open, closed, reopened]
      responses:
        '200':
          description: Ticket status updated